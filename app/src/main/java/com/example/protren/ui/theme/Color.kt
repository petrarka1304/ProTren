package com.example.protren.ui.theme

import androidx.compose.ui.graphics.Color

// ───────────────────────── PRIMARY / BRAND ─────────────────────────

// Akcent – turkus/mięta, nowoczesny i czytelny na ciemnym tle
val Primary = Color(0xFF37D2C5)
val OnPrimary = Color(0xFF00201D)
val PrimaryContainer = Color(0xFF005049)
val OnPrimaryContainer = Color(0xFF79F7EA)

// Delikatny secondary – np. do chipów, ikon drugoplanowych
val Secondary = Color(0xFF9AD4CD)

// ───────────────────────── DARK THEME ─────────────────────────

// Główne tło aplikacji (ekran)
val BackgroundDark = Color(0xFF0E1113)

// Tło kart, bottom sheetów, itp.
val SurfaceDark = Color(0xFF14181B)

// Wariant powierzchni – np. sekcje, listy
val SurfaceVariantDark = Color(0xFF1B2126)

// Tekst na ciemnym tle
val OnBackgroundDark = Color(0xFFE2E6EA)
val OnSurfaceDark = Color(0xFFD6DBE0)

// ───────────────────────── LIGHT THEME ─────────────────────────

// Główne tło w jasnym motywie
val BackgroundLight = Color(0xFFF5FBFA)

// Jasne powierzchnie – karty, appbar, bottom sheet
val SurfaceLight = Color(0xFFFFFFFF)

// Wariant powierzchni – np. sekcje o lekkim odcięciu
val SurfaceVariantLight = Color(0xFFE0F2F0)

// Tekst na jasnym tle
val OnBackgroundLight = Color(0xFF101415)
val OnSurfaceLight = Color(0xFF14181B)

// ───────────────────────── BORDER / DIVIDERS ─────────────────────────

val Outline = Color(0xFF2A3238)

// ───────────────────────── STANY BŁĘDÓW ─────────────────────────

val Error = Color(0xFFFFB4AB)
val OnError = Color(0xFF690005)
